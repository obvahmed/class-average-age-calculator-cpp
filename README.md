# Student Average Age Calculator (C++)

C++ console application that calculates the average age of students in a specific grade level.

## Features

* Accepts a grade level from the user
* Accepts the number of students in the class
* Collects the age of each student using a loop
* Calculates the total and average age of the class
* Displays the average age in the console
